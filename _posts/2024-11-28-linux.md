---
title: Linux
date: 2024-11-26 0:31:00
categories: [Sistemas, Linux]
tags: [sysdamin, lvm, pvs]
---

# Pues aqui vamos a hablar de administracion del SO en general, y si el post se hace muy largo, tendremos que hacer mas.  
Y aqui lo que sea

## LVM y discos

```bash
lsblk
pvs
vgs
lvs
vgchange -ay
lvextend -r -L +10G /dev/<VG>/<LV>
```

## ZFS

```bash
zpool status
zpool list
zfs list
zpool scrub <POOL>
```

## Journalctl

```bash
journalctl -xe
journalctl -u <SERVICIO> -f
journalctl --since "1 hour ago"
```

<!-- Fuentes integradas o revisadas: `Recuperar volumenes LVM`, `grupos_volumenes`, `Discos_comandas`, `chuleta_zpool.txt`, `jornalctl_revision_logs`. -->
